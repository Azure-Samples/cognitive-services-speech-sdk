{% if request.pass_through_data.x_voice_writer == "true" -%}
  {% if request.pass_through_data.subject == "PVVM-FTWM-English" -%}
    {% if request.pass_through_data.x_cns_language == "en-US" -%}
      Welcome to the 30-day free trial for Premium Visual Voice Mail. With Premium Visual Voice Mail, you can view, listen to, delete and manage all of your voice mail messages right on your mobile phone, and also enjoy the benefits of Voice Mail to Text. Voice Mail to Text converts your messages to text, so you can discreetly read your voice mails without listening to them. Also included with Premium Visual Voice Mail are enhanced mailbox features, such as a larger, 40 message mailbox, extended 5 minute voice mail record limit, Caller Dependent Greetings and the ability to receive fax messages directly on your phone.
    {% else -%}
      {% if request.pass_through_data.x_cns_language == "es-MX" -%}
        Bienvenido a la prueba gratuita por 30 días del Buzón de Voz Visual Premium. Con el Buzón de Voz Visual Premium, usted puede ver, escuchar, borrar y administrar todos sus mensajes de voz directo en su teléfono móvil así como disfrutar los beneficios de buzón de voz a texto. El buzón de voz a texto convierte sus mensajes en texto para que los pueda leer discretamente sin tener que escucharlos. El Buzón de Voz Visual Premium incluye además funciones optimizadas como un buzón más grande con capacidad para 40 mensajes, un límite extendido de 5 minutos para la grabación de mensajes de voz, saludos personalizados para quienes le llaman y la opción de recibir mensajes de fax directo en su teléfono.
      {% else -%}
        {% if request.pass_through_data.x_cns_language == "es-US" -%}
          Bienvenido a la prueba gratuita por 30 días del Buzón de Voz Visual Premium. Con el Buzón de Voz Visual Premium, usted puede ver, escuchar, borrar y administrar todos sus mensajes de voz directo en su teléfono móvil así como disfrutar los beneficios de buzón de voz a texto. El buzón de voz a texto convierte sus mensajes en texto para que los pueda leer discretamente sin tener que escucharlos. El Buzón de Voz Visual Premium incluye además funciones optimizadas como un buzón más grande con capacidad para 40 mensajes, un límite extendido de 5 minutos para la grabación de mensajes de voz, saludos personalizados para quienes le llaman y la opción de recibir mensajes de fax directo en su teléfono.
        {%- endif %}
      {%- endif %}
    {%- endif %}
  {% else -%}
    {% if request.pass_through_data.subject == "PVVM-FTEM-English" -%}
      {% if request.pass_through_data.x_cns_language == "en-US" -%}
       We hope you have enjoyed the Premium Visual Voice Mail service. Your 30-day trial ends tomorrow when you will be provided with two options, subscribe to Premium Visual Voice Mail for $2.99 per month with Voice Mail to Text, or revert back to Basic Visual Voice Mail.  If you choose to revert back to Basic Visual Voice Mail, rest assured, you will not lose any voice mails.  But if you have more than 20 voice mails in your mailbox, you will not be able to receive new ones until you clean out your mailbox below 20.
      {% else -%}
        {% if request.pass_through_data.x_cns_language == "es-MX" -%}
          Esperamos que haya disfrutado del servicio Buzón de Voz Visual Premium. Su prueba de 30 días termina mañana con la presentación de dos opciones: la suscripción al Buzón de Voz Visual Premium por $2.99 al mes con buzón de voz a texto, o el retorno al Buzón de Voz Visual Básico. Si usted escoge retornar al Buzón de Voz Básico, puede estar seguro que no perderá ningún mensaje de voz. Pero si tiene más de 20 mensajes en su buzón, no podrá recibir mensajes nuevos hasta que haya borrado algunos y tenga menos de 20.
        {% else -%}
          {% if request.pass_through_data.x_cns_language == "es-US" -%}
           Esperamos que haya disfrutado del servicio Buzón de Voz Visual Premium. Su prueba de 30 días termina mañana con la presentación de dos opciones: la suscripción al Buzón de Voz Visual Premium por $2.99 al mes con buzón de voz a texto, o el retorno al Buzón de Voz Visual Básico. Si usted escoge retornar al Buzón de Voz Básico, puede estar seguro que no perderá ningún mensaje de voz. Pero si tiene más de 20 mensajes en su buzón, no podrá recibir mensajes nuevos hasta que haya borrado algunos y tenga menos de 20.
          {%- endif %}
        {%- endif %}
      {%- endif %}
    {% else -%}
      {% if request.pass_through_data.subject == "PVVM-WM-English" -%}
        {% if request.pass_through_data.x_cns_language == "en-US" -%}
          Thank you for subscribing to Premium Visual Voice Mail.  With Premium Visual Voice Mail, you can view, listen to, delete and manage your voice mail messages right on your mobile phone, and also enjoy the benefits of Voice Mail to Text. Voice Mail to Text converts your messages to text, so you can discreetly read your voice mails without listening to them. Also included with Premium Visual Voice Mail are enhanced mailbox features, such as a larger, 40 message mailbox, extended 5 minute voice mail record limit, Caller Dependent Greetings and the ability to receive fax messages directly on your phone.
        {% else -%}
          {% if request.pass_through_data.x_cns_language == "es-MX" -%}
           Gracias por suscribirse al Buzón de Voz Visual Premium. Con el Buzón de Voz Visual Premium, usted puede ver, escuchar, borrar y administrar sus mensajes de voz directo en su teléfono móvil así como disfrutar los beneficios del buzón de voz a texto. El buzón de voz a texto convierte sus mensajes en texto para que pueda leerlos discretamente sin tener que escucharlos. El Buzón de Voz Visual Premium incluye además funciones optimizadas como un buzón más grande con capacidad para 40 mensajes, un límite extendido de 5 minutos para la grabación de mensajes de voz, saludos personalizados para quienes le llaman y la opción de recibir mensajes de fax directo en su teléfono.
          {% else -%}
            {% if request.pass_through_data.x_cns_language == "es-US" -%}
             Gracias por suscribirse al Buzón de Voz Visual Premium. Con el Buzón de Voz Visual Premium, usted puede ver, escuchar, borrar y administrar sus mensajes de voz directo en su teléfono móvil así como disfrutar los beneficios del buzón de voz a texto. El buzón de voz a texto convierte sus mensajes en texto para que pueda leerlos discretamente sin tener que escucharlos. El Buzón de Voz Visual Premium incluye además funciones optimizadas como un buzón más grande con capacidad para 40 mensajes, un límite extendido de 5 minutos para la grabación de mensajes de voz, saludos personalizados para quienes le llaman y la opción de recibir mensajes de fax directo en su teléfono.
            {%- endif %}
          {%- endif %}
        {%- endif %}
      {% else -%}
        {% if request.status == "Success" and request.recognition_result.conversion_status == "Transcribed" -%}
          {% if request.recognition_result.audio_truncated == true -%}
            {{ request.recognition_result.text }}...
          {% else -%}
            {{ request.recognition_result.text }}
          {%- endif %}
        {%- endif %}
      {%- endif %}
    {%- endif %}
  {%- endif %}
{% else -%}
  An error has occurred.
{%- endif %}