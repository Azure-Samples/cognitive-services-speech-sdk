{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Fetch Transcription from Queue",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}