{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Start Transcription by Timer",
      "type": "coreclr",
      "request": "attach",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}